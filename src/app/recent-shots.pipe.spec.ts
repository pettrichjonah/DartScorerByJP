import { RecentShotsPipe } from './recent-shots.pipe';

describe('RecentShotsPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentShotsPipe();
    expect(pipe).toBeTruthy();
  });
});
